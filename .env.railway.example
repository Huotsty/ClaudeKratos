# Generate a secure secret (minimum 32 characters)
# You can use this command to generate one:
# openssl rand -base64 32

KRATOS_SECRET=change-this-to-a-secure-secret-minimum-32-characters

# For Railway, use Railway's provided DATABASE_URL
# Format: postgres://user:password@host:port/database?sslmode=require
DATABASE_URL=postgres://kratos:secret@localhost:5432/kratos?sslmode=disable

# Mailtrap SMTP (for testing)
# Sign up at https://mailtrap.io and get credentials
MAILTRAP_USERNAME=your-mailtrap-username
MAILTRAP_PASSWORD=your-mailtrap-password
SMTP_CONNECTION_URI=smtp://${MAILTRAP_USERNAME}:${MAILTRAP_PASSWORD}@smtp.mailtrap.io:2525

# Or use SendGrid for production
# SMTP_CONNECTION_URI=smtp://apikey:YOUR_SENDGRID_API_KEY@smtp.sendgrid.net:587

# Or use AWS SES for production
# SMTP_CONNECTION_URI=smtp://USERNAME:PASSWORD@email-smtp.us-east-1.amazonaws.com:587

# Email configuration
SMTP_FROM_ADDRESS=noreply@yourdomain.com
SMTP_FROM_NAME=Your Application Name

# Frontend URL (update with your Railway frontend domain)
FRONTEND_URL=http://localhost:3000

# Kratos Public URL (update with your Railway Kratos domain)
KRATOS_PUBLIC_URL=http://localhost:4433
